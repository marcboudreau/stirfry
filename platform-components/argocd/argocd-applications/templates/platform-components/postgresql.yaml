apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql
  namespace: {{ .Values.namespace | default "platform" }}
  labels:
    type: platform
spec:
  project: default
  destination:
    namespace: {{ .Values.namespace | default "platform" }}
    server: https://kubernetes.default.svc
  source:
    path: platform-components/postgresql
    repoURL: https://github.com/marcboudreau/stirfry.git
    targetRevision: HEAD
    helm:
      releaseName: postgresql
      valueFiles:
        - base-values.yaml
        - {{ .Values.environmentName }}-values.yaml
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - ServerSideApply=true
